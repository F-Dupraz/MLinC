#ML in C

## Commits tell the story

Each commit represents a learning goal (e.g., "implement mat_mul", "debug backward pass"). 
Read through `git log` to see the progression from matrix ops → backprop → autodiff → architecture study.

Key milestones (tagged):
- `phase/1-complete` — XOR trains with explicit backprop
- `phase/2-complete` — Autodiff engine working, N-dim tensors
- `phase/3-complete` — ggml design study + refactor sketch
- `v1.0-release` — Portfolio-ready, docs + examples polished
